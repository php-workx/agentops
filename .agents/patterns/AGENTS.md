# Patterns

Reusable patterns discovered across sessions.

## Naming Convention
- `kebab-case.md` (descriptive name)

## Search
- `ao lookup --query "pattern name"` for ranked retrieval
- `Grep` in `.agents/patterns/` for exact matches
