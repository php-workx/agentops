# RPI Pipeline State

Pipeline state and scheduler outputs for RPI workflow.

## Key Files
- `next-work.jsonl` — pending work items
- `phased-state.json` — current pipeline phase and progress

## Search
- `Grep` in `.agents/rpi/` for run IDs or epic names
- Beads issues track progress: `bd ready`, `bd show <id>`
